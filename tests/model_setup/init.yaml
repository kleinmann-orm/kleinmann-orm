apps:
  models:
    default_connection: default
    models:
      - tests.testmodels
connections:
  default:
    credentials:
      file_path: /tmp/tester.sqlite
    engine: kleinmann.backends.sqlite
